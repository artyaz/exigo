/* Vitest stub for the `server-only` package, which throws on import outside
   an RSC server. Aliased in vitest.config.ts so server modules (e.g. the AI
   router) can be unit-tested in the node environment. */
export {};
