# SonarCloud automatic-analysis configuration.
#
# Exclude UX-review discrimination-bench fixtures and standalone HTML harnesses.
# These HTML files contain UX/accessibility defects ON PURPOSE (planted arm) or
# embed design-token CSS for offline review — linting them as product code fails
# the quality gate for defects/duplication that must stay for the loop to work.
#
# See agents/loop-forge/runs/2026-07-30-L004/recon/bench-results.md and
# agents/ux-review/LOOP.md §3 (βench) / §4 (publish harnesses).
sonar.exclusions=agents/**/recon/fixtures/**,agents/**/runs/**/bench/**,agents/**/runs/**/harnesses/**
