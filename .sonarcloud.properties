# SonarCloud automatic-analysis configuration.
#
# Exclude the ux-review discrimination-bench fixtures. These HTML files contain
# UX/accessibility defects ON PURPOSE: they are the planted arm of the test bench
# that gates the ux-review loop (canonical invariant C-001-can-01), and the loop
# is scored on whether it detects them. Linting them as product code fails the
# quality gate for defects that must stay exactly where they are.
#
# See agents/loop-forge/runs/2026-07-30-L004/recon/bench-results.md — SonarCloud
# independently flagged two of the eight planted defects, which is corroboration
# that the fixtures are realistic, not a reason to remove them.
sonar.exclusions=agents/**/recon/fixtures/**
