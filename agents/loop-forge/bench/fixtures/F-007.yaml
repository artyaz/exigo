# Fixture F-007 — planted_hitl — difficulty=hard
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-007
label: planted_hitl
target_domain: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI.'
context: 'Target domain is npm/Prisma/Convex dependency updates. The loop reads advisories, generates patch PRs, runs CI. Designed to look fully automated.'
difficulty: hard
source: "Inspired by exigo's auth/payments surface (convex/auth.ts, src/server/payments/)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      - 'request security team sign-off'

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that monitors dependencies for security advisories, opens update PRs, and merges on green CI. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'request security team sign-off'

ground_truth:
  planted_hitl_steps:
    - 'request security team sign-off before merging any dependency bump touching auth or payments'
  expected_admitted_criteria:
    - 'MUST_RESPECT: auto-merge minor/patch bumps when CI green'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'request security team sign-off for auth/payments bumps' MUST be quarantined — HITL on a specific class of changes"
