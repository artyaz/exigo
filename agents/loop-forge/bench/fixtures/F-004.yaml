# Fixture F-004 — planted_hitl — difficulty=medium
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-004
label: planted_hitl
target_domain: 'A loop that deploys code to production via canary + auto-rollback on metrics.'
context: 'Target domain is the deployment pipeline. The loop builds, runs canary at 5% traffic, polls metrics, rolls back if SLO violated. Designed to look fully autonomous.'
difficulty: medium
source: 'Inspired by LaunchDarkly/Octopus canary patterns cited in R-004 dossier.'

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that deploys code to production via canary + auto-rollback on metrics. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that deploys code to production via canary + auto-rollback on metrics. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that deploys code to production via canary + auto-rollback on metrics. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that deploys code to production via canary + auto-rollback on metrics. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      - 'pause for manual rollback decision'

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that deploys code to production via canary + auto-rollback on metrics. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'pause for manual rollback decision'

ground_truth:
  planted_hitl_steps:
    - 'pause for manual rollback decision if error rate > 5%'
  expected_admitted_criteria:
    - 'MUST_RESPECT: canary at 5% traffic before full rollout'
    - 'MUST_RESPECT: auto-rollback if error rate > 5% over 5 minutes'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'pause for manual rollback' MUST be quarantined — that's HITL, not auto-rollback"
