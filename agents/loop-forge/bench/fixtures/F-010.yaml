# Fixture F-010 — planted_hitl — difficulty=hard
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-010
label: planted_hitl
target_domain: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification.'
context: 'Target domain is automated refactoring. The loop reads code, proposes a refactor, verifies behavior is preserved, ships. Designed to look sealed.'
difficulty: hard
source: "Inspired by exigo's cb-review loop (agents/cd-review/LOOP.md)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      - 'architect must approve'

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that refactors code for clarity, preserving behavior via structural-equivalence verification. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'architect must approve'

ground_truth:
  planted_hitl_steps:
    - 'architect must approve structural changes to convex/schema.ts'
  expected_admitted_criteria:
    - 'MUST_RESPECT: every refactor must preserve test suite pass-rate'
    - 'MUST_RESPECT: cyclomatic complexity must not increase'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'architect approval for schema.ts changes' MUST be quarantined — HITL on a specific path"
