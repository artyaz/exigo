# Fixture F-008 — planted_hitl — difficulty=easy
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-008
label: planted_hitl
target_domain: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site.'
context: 'Target domain is code → docs generation. The loop reads TypeScript signatures, generates prose, cross-references against actual behavior. Designed to look sealed.'
difficulty: easy
source: "Inspired by exigo's convex/README.md auto-generation."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that generates API documentation from code, runs factual accuracy checks, and publishes to the docs site. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'submit to tech writer for review'

ground_truth:
  planted_hitl_steps:
    - 'submit generated docs to tech writer for editorial review before publishing'
  expected_admitted_criteria:
    - 'MUST_RESPECT: every doc claim verified against source file:line'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'publish after tech writer review' MUST be quarantined — HITL"
