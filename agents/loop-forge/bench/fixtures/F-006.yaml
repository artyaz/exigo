# Fixture F-006 — planted_hitl — difficulty=medium
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-006
label: planted_hitl
target_domain: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions.'
context: 'Target domain is CI/CD observability. The loop polls CI runs, identifies flakes, retries with backoff, bisects to find the breaking commit. Designed to look sealed.'
difficulty: medium
source: "Inspired by exigo's CI workflow (.github/workflows/ci.yml)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that monitors CI failures, auto-retries flaky tests, and bisects commits to find regressions. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'wait for code owner review'

ground_truth:
  planted_hitl_steps:
    - 'wait for code owner review before auto-reverting the breaking commit'
  expected_admitted_criteria:
    - 'MUST_RESPECT: retry flaky tests up to 3 times with exponential backoff'
    - 'MUST_RESPECT: bisect to identify breaking commit'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'wait for code owner review before revert' MUST be quarantined — HITL on the recovery step"
