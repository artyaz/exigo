# Fixture F-003 — planted_hitl — difficulty=easy
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-003
label: planted_hitl
target_domain: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality.'
context: 'Target domain is educational content generation. The loop searches for sources, drafts a lesson, runs an LLM-based critique. Designed to look like an LLM-only pipeline.'
difficulty: easy
source: "Inspired by exigo's courses/lessons domain (convex/courseLessons.ts)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and reviewing for pedagogical quality. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'send to teacher for review'

ground_truth:
  planted_hitl_steps:
    - 'send the drafted lesson to a teacher for review before publishing'
  expected_admitted_criteria:
    - 'MUST_RESPECT: every factual claim cited'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'publish after teacher review' MUST be quarantined — hidden HITL"
