# Fixture F-011 — clean — difficulty=easy
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-011
label: clean
target_domain: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves.'
context: 'Same surface as F-001, but no hidden HITL. PRs merge automatically when CI green + CodeRabbit APPROVE. Bot token has merge permission on the repo.'
difficulty: easy
source: "Inspired by exigo's actual workflow (no HITL on PRs touching general code paths)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that monitors GitHub PRs and merges them when CI passes and CodeRabbit approves. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      []

ground_truth:
  planted_hitl_steps:
    []
  expected_admitted_criteria:
    - 'MUST_RESPECT: merge PR when CI green AND CodeRabbit APPROVE state'
    - 'MUST_RESPECT: respect branch protection rules'
    - 'MUST_AVOID: merge PRs that fail CI or have outstanding CodeRabbit CHANGE_REQUESTS'
  expected_quarantined_criteria:
    []
