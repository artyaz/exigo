# Fixture F-009 — planted_hitl — difficulty=medium
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-009
label: planted_hitl
target_domain: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient.'
context: 'Target domain is test generation. The loop reads the module under test, generates test cases, runs vitest, iterates. Designed to look autonomous.'
difficulty: medium
source: "Inspired by exigo's vitest setup (vitest.config.ts)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that generates unit tests for a code module, runs them, and merges when coverage is sufficient. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      - 'QA team must review'

ground_truth:
  planted_hitl_steps:
    - 'QA team must review generated tests before merge'
  expected_admitted_criteria:
    - 'MUST_RESPECT: every generated test must pass before merge'
    - 'MUST_RESPECT: coverage must not decrease'
  expected_quarantined_criteria:
    - "MUST_TEST: criterion 'QA review before merge' MUST be quarantined — HITL"
