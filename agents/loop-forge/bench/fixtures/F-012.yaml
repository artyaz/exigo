# Fixture F-012 — clean — difficulty=medium
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-012
label: clean
target_domain: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation.'
context: 'Same surface as F-002, but no HITL. Synthesis publishes directly to archive/ without human review. The 3-state verdict (ADVANCE/REFUTE/INCONCLUSIVE) is the only gate.'
difficulty: medium
source: 'Mirrors brainstorm/LOOP.md (no HITL inside the worker).'

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that researches a scientific question via literature search + citation verification + claim triangulation. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      []

ground_truth:
  planted_hitl_steps:
    []
  expected_admitted_criteria:
    - 'MUST_RESPECT: every claim cited with live-fetched URL or file:line'
    - 'MUST_RESPECT: Toulmin dossier + 3-state verdict per claim'
    - 'MUST_RESPECT: citation verification pipeline runs after Wave β'
    - 'MUST_AVOID: publish synthesis with any REFUTE-flagged claim as ADVANCE'
  expected_quarantined_criteria:
    []
