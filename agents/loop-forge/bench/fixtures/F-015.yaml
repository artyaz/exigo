# Fixture F-015 — clean — difficulty=hard
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-015
label: clean
target_domain: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high.'
context: "Same surface as F-005, but no HITL. After 3 failed fix attempts, the loop falls back to labeling the issue 'needs-human-review' and moves on — it does NOT escalate to on-call."
difficulty: hard
source: 'Inspired by AgentGuard runtime verification pattern.'

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that triages incoming bug reports by labeling, assigning, and auto-fixing when confidence is high. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      []

ground_truth:
  planted_hitl_steps:
    []
  expected_admitted_criteria:
    - 'MUST_RESPECT: classify severity using LLM with citation-backed rubric'
    - 'MUST_RESPECT: auto-fix only when code is in well-tested module'
    - "MUST_RESPECT: after 3 failed attempts, label issue 'needs-human-review' and move on (NOT a HITL step — the loop continues)"
  expected_quarantined_criteria:
    []
