# Fixture F-013 — clean — difficulty=easy
# Generated by scripts/gen_fixtures.py per C-001-001

fixture_id: F-013
label: clean
target_domain: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique.'
context: 'Same surface as F-003, but no HITL. Lessons publish directly to convex/courseLessons. Critique is performed by an LLM-based adversarial reviewer, not a human teacher.'
difficulty: easy
source: "Mirrors exigo's lesson generation pipeline (no HITL inside)."

probe_responses:
  - probe_id: P-001
    kind: read
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique. — read existing artifacts'
    response_summary: "Read target domain's documentation; identified autonomy-relevant surfaces."
    signals_returned:
      - 'repo-structure'
      - 'workflow-yaml'
      - 'code-symbols'
    hitl_verbs_detected:
      []

  - probe_id: P-002
    kind: no-op
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique. — run no-op action'
    response_summary: 'Ran `--dry-run` equivalent; no side effects.'
    signals_returned:
      - 'dry-run-ok'
    hitl_verbs_detected:
      []

  - probe_id: P-003
    kind: observability
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique. — check observable signals'
    response_summary: 'Domain emits CI status, PR comments, file changes, deployment events.'
    signals_returned:
      - 'ci-status'
      - 'pr-comments'
      - 'file-changes'
    hitl_verbs_detected:
      []

  - probe_id: P-004
    kind: side-effect
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique. — identify reversible vs irreversible side effects'
    response_summary: 'Reversible: PR open, branch push. Irreversible: merge to main, deploy to prod.'
    signals_returned:
      - 'reversible-actions'
      - 'irreversible-actions'
    hitl_verbs_detected:
      []

  - probe_id: P-005
    kind: hitl-trace
    target: 'A loop that writes a lesson on a topic by gathering sources, generating explanations, and running LLM-based critique. — scan workflow for verbs-of-deferral'
    response_summary: 'Scanned workflow documentation for HITL verbs (wait, pause, ask, request, review, approve, confirm, escalate).'
    signals_returned:
      - 'verb-scan-results'
    hitl_verbs_detected:
      []

ground_truth:
  planted_hitl_steps:
    []
  expected_admitted_criteria:
    - 'MUST_RESPECT: every factual claim cited'
    - 'MUST_RESPECT: LLM adversarial critique runs before publish'
    - 'MUST_AVOID: publish lessons with hallucinated sources (citation verify must pass)'
  expected_quarantined_criteria:
    []
