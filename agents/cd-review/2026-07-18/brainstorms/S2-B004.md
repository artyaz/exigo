# Decision Package — S2-B004

## Issue Packet (from L1)

```text
BRAIN_ID: S2-B004
TRIGGER: clarity | structure | inconsistency
NORTH_STAR_HURT: readable, short, consistent
LOCATION: convex/courseAi.ts (~1120 lines) — 9 public actions each copy getAiClient/model/observability/parse; teach/verify/clarify also exist as Next SSE routes
WHAT_I_TRIED_MENTALLY: extract runGeminiJson helper; delete unused actions if routes supersede
QUESTION: What is the smallest structure fix that raises readability without a gateway rewrite?
```

## Recommendation
- Approach name: **Extract AI primitives + mark dual-path ownership**
- One-paragraph rationale: `courseAi.ts` is a god file of copy-pasted Gemini call + PostHog + safeParseJson. Parallel teaching lives in `src/app/api/learn/teach/route.ts` while `teachLesson` action remains. North star wants shorter and consistent — first extract local helpers in the same file (or `convex/lib/geminiCourse.ts`): `requireCourseAiAuth`, `generateText`, `generateJson`. Second, inventory which actions are still called from `src/app/actions/learn.ts` vs dead vs duplicated by SSE; delete or `@deprecated` comment dead paths. Do **not** build a full AI gateway in this slice.
- Why not full split into 9 files immediately: file-per-action can help later but extraction of the repeated 15-line block is the 80% win.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Helpers in-file or tiny gemini helper module; dual-path matrix in audit only | Surgical readability | Still one large file | 4/5 | S–M |
| B | Split courseAi into courseAi/{normalize,baseline,module,teach,...}.ts | Clear modules | Touch many imports; bigger PR | 4/5 | L |
| C | Delete Convex teach/verify/clarify if only SSE used | Huge delete win | learn.ts still calls them — verify before delete | 5/5 if unused | S if proven |

### Minimal implementation sketch
- Confirm call graph from `src/app/actions/learn.ts` (teachLesson, verifyInput, clarifyConcept, summarizeLesson are still wired).
- Extract:
  ```ts
  async function runGemini(params: { auth; prompt; model?; json?: boolean })
  ```
- Leave orchestration of DB writes in each action.
- Add file header comment listing canonical paths: “Streaming teach = Next route; non-stream actions = this file / server actions”.
- What NOT to do: Don't move Gemini into Next-only and break Convex actions without migration plan.

### Skills applied
- Karpathy guidelines (simplicity, surgical); architecture boundaries

### Research notes
- AGENTS.md: AI-heavy SSE in Next API routes; Convex for domain. courseAi straddles both — technical debt.

### Residual risks
- Helpers can become a junk drawer; keep them <80 lines.

### Suggested finding severity / title (for L1)
- **P1/P2** — courseAi god file + duplicated AI call pattern; dual teach stack (Convex action vs SSE route).
