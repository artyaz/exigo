## Decision Package — S9-B001

### Recommendation
- Approach name: **Truthful scope + keep dual stack explicit (no mega-migration)**
- One-paragraph rationale: The `AiProvider` layer is real and used by `src/app/api/generate/*`, but comments claim *every* model call goes through it while learn/test/title routes and Convex actions still construct `GoogleGenAI` directly. The clarity fix is to correct the liar surface (module docs + `index.ts` comment) and name the two intentional paths: (1) Next playground/generate via `resolveAiProvider`, (2) product SSE/Convex via direct Gemini + prompts. Full migration is out of slice scope and fails YAGNI until a concrete second consumer needs the abstraction on those paths.
- Why not the alternatives: Migrating all routes now is a large, behavior-risky rewrite spanning S8/S2. Deleting the abstraction throws away working multi-provider support already wired to settings.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Truthful docs + optional `// dual-path` note at resolve entry | Immediate clarity; no behavior change; short | Does not unify stacks | 5/5 | S |
| B | Migrate all Next AI routes onto `resolveAiProvider` | One server path; honors settings everywhere | Large S8 change; Convex still separate; risk to SSE/observability | 3/5 | L |
| C | Delete AiProvider; only Gemini | Fewer files | Loses custom OpenAI endpoints + settings feature | 1/5 | M |

### Minimal implementation sketch
- Files to touch: `src/server/ai/types.ts`, `src/server/ai/index.ts`, `src/server/ai/resolve.ts` (header comments only)
- Steps:
  1. Rewrite top-of-file claims: abstraction covers **generate/* (and any caller that imports `~/server/ai`)**; default product AI elsewhere remains Gemini + Convex prompts / route-local clients.
  2. State why: custom provider is a user setting used by exercise generation playground paths; course/test streaming not yet routed here.
  3. Do **not** migrate S8 routes in this pack.
- What NOT to do: Full gateway rewrite; move Convex AI into `src/server` (runtime boundary).

### Skills applied
- Base: brainstorming (multi-option + YAGNI)
- Domain: Karpathy simplicity; API/AI route consistency (scope honesty)

### Research notes
- In-repo evidence: `resolveAiProvider` imported only under `src/app/api/generate/*`; direct `GoogleGenAI` in learn/tests/knowledge routes and `convex/courseAi.ts`.
- Key takeaway: dual stack is architectural fact today; documentation debt is the fixable P1 clarity bug in S9 ownership.

### Residual risks
- Future engineers still invent a third path; mitigated by honest module header pointing to the two paths.

### Suggested finding severity / title (for L1)
- **P1** — Liar “every model call” claim; incomplete AiProvider adoption (document dual path)
