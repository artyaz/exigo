## Decision Package — S7-B001

### Recommendation
- Approach name: **B — Split by phase into colocated modules (keep route shell thin)**
- One-paragraph rationale: `page.tsx` is ~2080 lines holding shell routing, baseline card arena, module generation spinner, lesson teach/checkpoint/clarify runtime, and summary — five mental models in one file. The natural domain cut already exists as named sections (`BaselinePhase`, `GeneratingPhase`, `LessonPhase`, `SummaryPhase`, helpers). Surgical extraction into sibling files under the route (or `_components/learn/course/`) preserves behavior, unlocks parallel review/fix, and matches App Router colocation without inventing a framework.
- Why not the alternatives: A only renames sections (still unreadable). C (full feature module + shared state machine) is a multi-day rewrite and YAGNI for clarity audit.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Comment/section polish only | Zero risk | File still ~2k; no real readability win | 1/5 | S |
| B | Extract phase components + shared hooks/helpers | Immediate navigability; matches existing boundaries; surgical | Need careful export of types/helpers | 5/5 | M |
| C | Redesign as course runtime package + reducer | Clean long-term | Large redesign, high regression risk on SSE/checkpoints | 3/5 | L |

### Minimal implementation sketch
- Files to touch:
  - `src/app/spaces/[spaceId]/learn/[courseId]/page.tsx` (thin shell)
  - New: `.../BaselinePhase.tsx`, `GeneratingPhase.tsx`, `LessonPhase.tsx`, `SummaryPhase.tsx`
  - New (optional shared): `.../focusMode.ts` or `useActiveFocusTargets.ts`, `FocusModeToggle.tsx`, `actionResult.ts` (unwrap helpers)
  - Possibly later: extract clarification from LessonPhase (see S7-B004)
- Steps (bullet, surgical):
  1. Move pure helpers (`unwrapActionResult`, `serializeCheckpointMap`, `serializeVerification`, `useActiveFocusTargets`, `FocusModeToggle`) to small shared modules next to the route or under `src/app/_components/learn/`.
  2. Cut each `// ─── X Phase ───` function into its own file; re-export if tests need them (none currently).
  3. Leave `CoursePage` as: auth + queries + phase switch + `CourseTutor`.
  4. Do **not** change SSE/action contracts in this pass.
- What NOT to do:
  - No new global store / context for course state.
  - No merging with Convex orchestrator types.
  - No behavior changes to baseline card physics or checkpoint restore.

### Skills applied
- Base: brainstorming (structured multi-option + YAGNI)
- Domain: React/UI structure (frontend-patterns: composition, colocate state); Karpathy simplicity / surgical changes; architecture boundaries

### Research notes
- Sources: local `frontend-patterns` skill (composition, colocate); `coding-guidelines` (simplicity, surgical)
- Key takeaway: split along *already named* phase boundaries; do not invent a new abstraction layer.

### Residual risks
- Large move diff may confuse blame; prefer pure cut-paste per file.
- `LessonPhase` alone remains huge (~1.1k lines) — follow with S7-B004.

### Suggested finding severity / title (for L1)
- **P1** — Course learn `page.tsx` is a multi-phase god page (~2080 lines)
