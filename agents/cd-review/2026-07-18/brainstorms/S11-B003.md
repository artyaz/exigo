# Decision Package — S11-B003

## Issue Packet (from L1)

- **BRAIN_ID:** S11-B003
- **TRIGGER:** inconsistency | clarity
- **NORTH_STAR_HURT:** consistency, readability, correctness tooling
- **LOCATION:**
  - `src/app/tests/page.tsx` (eslint-disable + @ts-nocheck; stack cards)
  - `src/app/_components/tests/TestGrid.tsx` (typed stack cards + filters)
- **SYMPTOM:** Global tests list reimplements the 3D stack card UI with type-checking and lint fully disabled; space-scoped grid is the advanced, typed version. `hashCode` duplicated in both (+ elsewhere).
- **EVIDENCE:** File starts with `/* eslint-disable */` and `// @ts-nocheck`. Parallel stack/hover/spotlight markup vs TestGrid.
- **QUESTION:** Delete global page stack and reuse TestGrid, or only re-enable types?

---

## Decision Package — S11-B003

### Recommendation
- **Approach name:** A — Re-type `tests/page.tsx` and extract shared `TestStackCard` presentation; prefer adapting TestGrid data shape or a thin shared card
- **Rationale:** `@ts-nocheck` is a structural smell that hides real errors. Duplicated stack visuals will drift (already slightly different padding/rotation). Smallest durable fix is a shared presentational card + typed page; full deletion of `/tests` route is product scope, not clarity.
- **Why not alternatives:** B (only remove nocheck) still leaves UI duplication. C (delete `/tests` page) may be product-valid later but out of pure clarity without product confirmation.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Shared card + restore types on list page | Consistency; lint honesty | listAll shape differs from space docs | 5/5 | M |
| B | Only remove nocheck/eslint-disable | Quick | Stack still duplicated | 3/5 | S |
| C | Redirect `/tests` → spaces | Deletes debt | Product decision | 4/5 | S (if accepted) |

### Minimal implementation sketch
- Files:
  - `src/app/_components/tests/TestStackCard.tsx` (new) — stack depth, hover, spotlight, status badge
  - `src/app/tests/page.tsx` — remove nocheck; map `listAll` rows into card props
  - `src/app/_components/tests/TestGrid.tsx` — consume shared card
- Steps:
  1. Define a minimal `TestCardModel` interface (id, spaceName?, topic, type, answered, target, questionCount, createdAt, href).
  2. Move stack visual into one component.
  3. Delete local `hashCode` copies in these two files (single util in the component file).
  4. Fix any typing issues that `@ts-nocheck` was hiding.
- What NOT to do:
  - Don’t port TestGrid’s sort/filter into the global page unless needed.
  - Don’t expand Prisma/tRPC.

### Skills applied
- Base: brainstorming + YAGNI
- Domain: consistency / code review quality

### Research notes
- None external.

### Residual risks
- `listAll` may return a slightly different shape than `Doc<"tests">` — adapt with an explicit mapper rather than force-casting.

### Suggested finding severity / title (for L1)
- **P1** — `/tests` list disables all type/lint checks and forks TestGrid stack UI
