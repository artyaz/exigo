## Decision Package — S1-B004

### Recommendation
- Approach name: **Delete fictional limitEnforcement handlers; replace with pure-unit tests of real helpers + thin ownership checks**
- One-paragraph rationale: `limitEnforcement.test.ts` reimplements local `createSpaceHandler` / `addKnowledgePieceHandler` / `createEmptyTestHandler` that (a) take plan as a client `args.plan` spoof, (b) count tests per space not per user-month, (c) never import production mutations. CI green does not protect production. Pre-mortem: free test limit of 3 is never boundary-tested; mock loads 10 rows and still "passes." Prefer testing pure functions already exported (`getLimitsForAccessLevel`, `parseSlugToAccessLevel`, and when extracted, count helpers) and delete the ghost integration theater.
- Why not the alternatives: Full Convex integration tests need a test backend harness (higher cost); keeping the file as-is is harmful confidence.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Delete ghost handlers; keep/expand pure unit tests | Honest suite; short | Less "integration" feel | 5/5 | S |
| B | Extract real enforcement helpers from spaces/tests and unit-test those | Tests real code | Touches S3 files | 4/5 | M |
| C | Keep structure, wire to real imported handlers | Closer to prod | Handlers need heavy Convex ctx mock | 3/5 | M |

### Minimal implementation sketch
- Files: `convex/limitEnforcement.test.ts` (rewrite/slim), `convex/planLimits.test.ts` (keep).
- Steps:
  1. Remove local handler copies and privateMetadata plan injection.
  2. Keep exploit-style tests only if they call shared pure auth predicate helpers (if none exist, drop or add one-line helpers).
  3. Assert free/pro/educator limits and `parseSlugToAccessLevel` edge cases (already partly in planLimits.test).
  4. Optionally move calendar-month count pure functions to shared/testable module in a later slice.
- What NOT to do: Do not mock `ctx.auth.getUserIdentity` with plan in privateMetadata — production reads Convex `subscriptions`, not Clerk metadata, for access.

### Skills applied
- Base: brainstorming
- Domain: TDD / systematic debugging (tests must fail when prod breaks)

### Research notes
- "Delete-test" adversarial trick: if deleting the test file changes production risk zero, the tests were theater.

### Residual risks
- Temporary coverage dip on spaces mutations until S3 adds real tests — acceptable vs false green.

### Suggested finding severity / title (for L1)
- **P1** — Liar limit-enforcement tests (ghost handlers, wrong meter, plan spoof)
