# Decision Package — S3-B007

## Issue Packet (from L1)

```
BRAIN_ID: S3-B007
TRIGGER: clarity | bug (test debt)
NORTH_STAR_HURT: clear, correct, short
LOCATION: convex/tests.test.ts (~980 lines)
SYMPTOM: Most tests exercise local shadow handlers that do not match production auth or schema
EVIDENCE:
  - updateStatusHandler checks test.userId (schema: tests rarely set userId; prod checks space.userId)
  - createHandler omits plan limits, auth, questionCount
  - Only listAll helpers + listAllHandlerFn test real exports
CONSTRAINTS: S3; vitest globals; prefer pure unit tests
WHAT_I_TRIED_MENTALLY: delete shadow suites; keep real helper + handler tests
QUESTION: How much of tests.test.ts to keep?
```

## Recommendation
- Approach name: **A — Delete shadow-handler suites; keep/expand real-export tests only**
- One-paragraph rationale: Shadow handlers are liar documentation: they encode an older ownership model (`test.userId`) and invent APIs that never ran. They inflate green CI without protecting production. Keep tests of exported pure helpers (`sortTestsByCreationDesc`, `countAnsweredQuestions`, `enrichTestForList`) and the real `_handler` probes for `listAll` (and optionally create/updateStatus if extracted helpers). Delete the rest (~700 lines).
- Why not B (rewrite shadows to match prod): high effort, still brittle vs Convex. Why not C (convex-test full integration): valuable later but not required for this clarity win.

## Approaches considered

| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Delete liar shadows; keep real helper/handler tests | Massive brevity; honest coverage | Coverage % drops | 5/5 | S |
| B | Align shadows with current handlers | Keeps line count | Still not testing production | 2/5 | M |
| C | convex-test integration suite | High confidence | Heavier setup | 4/5 | L |

## Minimal implementation sketch
- Files: `convex/tests.test.ts` only
- Steps:
  1. Keep `listAll helper functions` and `listAll handler` describe blocks.
  2. Delete create/updateStatus/get/getForSpace shadow suites and integration scenarios built on them.
  3. Optionally extract create limit-check pure function later for real unit tests — not required now.
- What NOT to do: don't chase coverage % by testing mocks of mocks.

## Skills applied
- Base: YAGNI; Karpathy simplicity
- Domain: TDD / review quality — delete-test adversarial trick

## Residual risks
- Coverage dashboard regresses; document that line count was false confidence.

## Suggested finding severity / title (for L1)
- **P2** — `tests.test.ts` mostly tests non-production shadow handlers with wrong auth model
