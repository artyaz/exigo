## Decision Package — S7-B004

### Recommendation
- Approach name: **B — Extract hooks by concern: teach stream, checkpoints, clarifications**
- One-paragraph rationale: After S7-B001, `LessonPhase` still owns progressive teach SSE, checkpoint verify/skip/advance, DB restore, text-selection bubble, clarification SSE threads, and "feels hard" context menu. That is the hottest path in Learn UI. Clarity comes from extracting *state machines* into hooks (`useLessonTeachStream`, `useLessonCheckpoints`, `useLessonClarifications`) while leaving JSX composition in the phase component — not from a giant Context.
- Why not the alternatives: A is only cosmetic. C (full headless lesson controller class) over-abstracts and fights React/Convex query restore patterns already in `lessonCheckpoints.ts`.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Leave LessonPhase after file split only | Less work | Still ~1100 lines; untestable SSE glue | 2/5 | S |
| B | Concern hooks + keep phase as view | Readable; unit-test pure transitions; reuses `~/lib/lessonCheckpoints` | Careful dependency ordering for restore vs teach-start | 5/5 | M |
| C | Single LessonRuntime context provider | One bag of state | Prop-drilling replaced by invisible coupling | 2/5 | L |

### Minimal implementation sketch
- Files to touch:
  - `LessonPhase` (after B001 extract)
  - New hooks under `src/app/_components/learn/` or route `_hooks/`:
    - `useLessonTeachStream.ts` — teach fetch/SSE, `fullText`, `isTeaching`, start-once ref
    - `useLessonCheckpoints.ts` — revealedCount, answered map, verify/skip/advance, restore sync
    - `useLessonClarifications.ts` — selectionState, threads Map, streamClarification, restore from messages
  - Share SSE helper from S7-B002
- Steps:
  1. Freeze restore rules (waitingForLocalAdvance etc.) — move as-is, no logic rewrite.
  2. Extract clarifications first (most self-contained + dead `pendingInThreadReply` / `isClarifySubmitting` cleanup).
  3. Extract teach stream; then checkpoints.
  4. Delete liar comment "rendered via portal — see below" (threads use `blockAppendages`).
- What NOT to do:
  - Do not change checkpoint persistence shape or clarify API body.
  - Do not merge clarification markdown with `LessonMarkdown` in this pass.

### Skills applied
- Base: brainstorming + YAGNI
- Domain: React/UI (hooks for state, colocate); hot-path structure; AI streaming client consistency (with B002)

### Research notes
- Existing pure logic already extracted: `~/lib/lessonCheckpoints` — good boundary to mirror.
- Key takeaway: UI page still owns stream I/O that belongs next to that pure restore layer.

### Residual risks
- Teach auto-start effect + restore race is subtle; preserve effect deps and refs exactly.
- Clarification thread Map updates are performance-sensitive during streaming.

### Suggested finding severity / title (for L1)
- **P1** — `LessonPhase` mixes teach SSE, checkpoints, and clarification runtime
