# Decision Package — S11-B002

## Issue Packet (from L1)

- **BRAIN_ID:** S11-B002
- **TRIGGER:** structure | clarity
- **NORTH_STAR_HURT:** readability, clarity, brevity
- **LOCATION:** `src/app/tests/[testId]/page.tsx` (~985 lines)
- **SYMPTOM:** Single page owns SSE question generation, answer validation, chat, “feels hard”, toast/context menu, arena resize, keyboard nav, card-stack layout, and a local markdown renderer.
- **EVIDENCE:** Multiple `eslint-disable-next-line react-hooks/exhaustive-deps`; local `renderMarkdown` / `renderInlineMarkdown` (~80 lines); three independent fetch pipelines; sibling course learn page duplicates `cardHash` pattern.
- **CONSTRAINTS:** Clarity-first surgical splits; no product features; tests UI owned by S11.
- **QUESTION:** How far to split on the first fix pass?

---

## Decision Package — S11-B002

### Recommendation
- **Approach name:** B — Extract pure helpers + 2–3 presentational/hooks modules; keep page as orchestrator
- **Rationale:** Full rewrite (C) risks behavior drift on a hot path (SSE + evaluation + auto-advance). Doing nothing (A) leaves the highest-churn UI file in the slice unreadable. Extracting (1) markdown helpers, (2) generation/validation fetch hooks or modules, (3) chat sidebar component gives immediate readability without redesigning the card arena.
- **Why not alternatives:** A fails the north star. C is a rewrite-for-taste risk.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Leave monolithic | Zero risk | Archaeology forever | 1/5 | — |
| B | Surgical extract (md + hooks + ChatSidebar) | Shorter page; testable pure md; clear boundaries | Need careful prop surface | 5/5 | M |
| C | Full “TestRuntime” redesign | Cleanest end state | High regression surface | 3/5 | L |

### Minimal implementation sketch
- Files to touch (new under `src/app/_components/tests/` or colocate):
  - `markdown.tsx` — move `renderInlineMarkdown` / `renderMarkdown`
  - `useTestQuestionGeneration.ts` — background SSE loop + error/retry
  - `TestChatSidebar.tsx` — messages + input + feels-hard menu consumers
  - slim `tests/[testId]/page.tsx` as composition root for arena + header
- Steps:
  1. Move markdown pure functions first (zero behavior risk).
  2. Extract generation effect into a hook that returns `{ isGeneratingNext, genError, retry }`.
  3. Extract chat column + context menu + toast into one component taking question/test ids.
  4. Leave card-stack motion in the page until stable (hot-path UI).
- What NOT to do:
  - Don’t unify with course learn page’s card stack in the same PR.
  - Don’t introduce a global state library.

### Skills applied
- Base: brainstorming + YAGNI
- Domain: frontend patterns (container/presentational only when it shortens); Karpathy simplicity (surgical)

### Research notes
- Sibling: `spaces/.../learn/[courseId]/page.tsx` also inlines `cardHash` — out of S11 ownership; note as cross-slice residual only.

### Residual risks
- Hook extraction can re-trigger generation if deps wrong — keep the existing `lastGeneratedForCount` guard semantics.

### Suggested finding severity / title (for L1)
- **P1** — Test detail page is a god component (~985 lines) mixing network, markdown, and arena UI
