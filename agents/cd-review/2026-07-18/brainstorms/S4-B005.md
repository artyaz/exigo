# Decision Package — S4-B005

## Issue Packet (input)
- **BRAIN_ID:** S4-B005
- **TRIGGER:** clarity + inconsistency
- **NORTH_STAR_HURT:** clarity, consistency
- **LOCATION:** `runtime/types.ts` `Reaction.allowSequentialWrite`; `runtime/vm.ts` stores it on `CompiledReaction` but never reads it; `runtime/validate.ts` only uses it to silence multi-writer warnings
- **SYMPTOM:** Name reads like a runtime permission. Reality: lint opt-out. VM dead field misleads readers of the hot path.
- **QUESTION:** Enforce sequential writes at runtime, or demote the flag to “warning silence only” and stop compiling it into the VM?

---

## Decision Package — S4-B005

### Recommendation
- **Approach name:** B — Demote to validator-only silence flag; drop dead VM field; rename or comment truthfully
- **One-paragraph rationale:** Current behavior is already “silence warning only” (validate message says so). Enforcing isolation at runtime (A) would change multi-reaction folds that intentionally write the same key across sequential reactions for one event — a common obs-fold pattern (`allowSequentialWrite: true` in AUTHORING.md). The clarity fix is: stop lying in the VM, document the flag as order-dependency acknowledgment, optionally rename later to `acknowledgeCrossWrite` if renaming is cheap across markup (`seq` flag). Prefer minimal: remove unused `allowSequentialWrite` from `CompiledReaction` / constructor map; keep on `Reaction` type for validate + markup.
- **Why not the alternatives:** Runtime isolation (A) is a behavior change for intentional folds. Leave as-is (C) keeps the dead field trap.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Enforce: second writer without flag throws | Real semantics | Breaks intentional multi-reaction folds | 2/5 | M |
| B | Validator-only; strip dead VM storage; honest comment | Matches reality, shorter VM | Rename deferred | **5/5** | S |
| C | No change | — | Liar structure remains | 1/5 | — |

### Minimal implementation sketch
- Files: `runtime/vm.ts`, optionally `types.ts` JSDoc on the flag
- Steps:
  1. Remove `allowSequentialWrite` from `CompiledReaction` and the map in constructor.
  2. JSDoc on `Reaction.allowSequentialWrite`: “Acknowledges order-dependent multi-reaction writes; silences validator warning only — not a runtime switch.”
  3. Leave markup `seq` flag mapping unchanged.
- What NOT to do: change validate warning logic; rename markup `seq` in this pack unless fixers have bandwidth.

### Skills applied
- coding-guidelines: delete dead code; don’t invent features
- clarity: names match behavior

### Residual risks
- Authors still believe `seq` changes VM atomicity — docs must be clear.

### Suggested finding severity / title (for L1)
- **P2** — `allowSequentialWrite` is a validator mute switch stored as dead VM state
