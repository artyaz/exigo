# Decision Package — S11-B004

## Issue Packet (from L1)

- **BRAIN_ID:** S11-B004
- **TRIGGER:** inconsistency | security/money touchpoint
- **NORTH_STAR_HURT:** consistency, clarity
- **LOCATION:**
  - `src/app/checkout/page.tsx` (inline Paddle Initialize + open with frameTarget)
  - `src/app/PaddleCheckoutReceiver.tsx` (overlay open for `_ptxn` query; owns `PaddleSDK` types)
- **SYMPTOM:** Two clients load paddle.js, sandbox detection via `token.startsWith("test_")`, and `Initialize`/`Checkout.open` with slightly different options and event handling.
- **QUESTION:** Shared helper vs leave both as thin wrappers?

---

## Decision Package — S11-B004

### Recommendation
- **Approach name:** A — Extract `loadPaddle(token)` + shared types; keep two thin call sites
- **Rationale:** Money path warrants one sandbox/init rule. Full merge of inline vs `_ptxn` receiver UX is unnecessary — different display modes. Shared init prevents divergent Environment/event handling bugs.
- **Why not alternatives:** B (copy-paste normalize comments only) doesn’t prevent drift. C (one mega checkout component) couples unrelated entry points.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Shared paddle client helper | Single sandbox rule; typed Window | Small new module | 5/5 | S |
| B | Leave duplicated | Zero files | Drift risk on payments | 2/5 | — |
| C | Unify both UIs | One surface | Wrong abstraction (inline vs overlay) | 2/5 | M |

### Minimal implementation sketch
- Files:
  - `src/lib/paddleClient.ts` (or `src/app/_components/paddle.ts`) — `PaddleSDK` types, `ensurePaddleEnvironment(token)`, `initializePaddle(token, eventCallback)`
  - Update `checkout/page.tsx` and `PaddleCheckoutReceiver.tsx` to call helper
- Steps:
  1. Move global `Window.Paddle` declaration to one module.
  2. Helper: if `token.startsWith("test_")` → sandbox; then Initialize once-guard if needed.
  3. Call sites only pass open options (inline settings vs default overlay).
- What NOT to do:
  - Don’t change redirect/`_ptxn` product behavior in the same change unless fixing a confirmed bug.
  - Don’t put secrets in client beyond existing public client token.

### Skills applied
- Base: brainstorming + YAGNI
- Domain: security/money touchpoint — least privilege unchanged; consistency of payment init

### Research notes
- Paddle.js v2 pattern already partially correct; focus is dedupe not redesign.

### Residual risks
- Double-Initialize if both components mount — keep isCheckoutOpened guards; optionally singleton init flag in helper.

### Suggested finding severity / title (for L1)
- **P2** — Paddle checkout init duplicated across inline checkout and `_ptxn` receiver
