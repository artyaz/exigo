# Decision Package — S2-B006

## Issue Packet (from L1)

```text
BRAIN_ID: S2-B006
TRIGGER: clarity | inconsistency
NORTH_STAR_HURT: short, clear, consistent
LOCATION:
  - courses.create === createCourseFromNormalized (identical)
  - courses.updatePhase public dead code (no callers)
  - JSON strings: masteryGoals, verifierLogs, subTopics, baselineResults vs typed checkpointStates
  - seedPrompts one-shot throw; no upsert
  - courseTutor searchMemories filters vector by spaceId only (userId filterField unused)
  - debugPlan in S2 slice (env gate OK-ish)
CONSTRAINTS: schema migrations are costly; prefer clarity without big migration unless high value
QUESTION: Which brevity/schema cleanups are worth doing this iteration?
```

## Recommendation
- Approach name: **Delete twins & dead exports now; defer schema retype; fix memory filter**
- One-paragraph rationale: Identical `create` / `createCourseFromNormalized` and unused `updatePhase` are pure noise — delete/alias. JSON-as-string is real tech debt (parse try/catch everywhere) but migrating Convex schema mid-loop is large; extract shared `parseJsonField<T>` helper first, typed schema later. `searchMemories` should filter `userId` since vectorIndex declares it — defensive even with single-user spaces. seedPrompts upsert is operational P3.
- Why not full schema migration now: high risk, low north-star urgency vs auth/state machine P0–P1.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Delete dead/dup APIs + parseJson helper + memory userId filter | Fast clarity/correctness | Schema still messy | 4/5 | S |
| B | Full retype subTopics/masteryGoals to Convex objects | Type safety | Migration + all readers | 4/5 | L |
| C | Do nothing | — | Debt compounds | 1/5 | — |

### Minimal implementation sketch
1. Remove `courses.create` or make it call shared insert helper; keep the name used by frontend (`createCourseFromNormalized`).
2. Remove public `updatePhase` (keep internal).
3. `searchMemories` filter: `q.eq("spaceId", ...).eq("userId", args.userId)` if API allows compound filter — check Convex vectorSearch filter constraints (single eq or and).
4. Optional: `safeParseJsonField` shared between courseAi summarize/verify.
5. seed: add `upsert` internalMutation later — not blocking.
- What NOT to do: Giant schema PR mixed with auth fixes.

### Skills applied
- Karpathy simplicity; YAGNI

### Residual risks
- Removing `create` if any external client uses it — grep first (LearnTab uses createCourseFromNormalized).

### Suggested finding severity / title (for L1)
- **P2** — Duplicate/dead course APIs; JSON blob fields; vector memory filter misses userId.
