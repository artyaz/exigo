## Decision Package — S9-B005

### Recommendation
- Approach name: **Mark dead T3 path explicitly; delete Post router only if product confirms unused UI**
- One-paragraph rationale: `src/server/db.ts`, `api/trpc.ts`, `api/root.ts`, `api/routers/post.ts` are classic T3 scaffolding. AGENTS.md says Prisma is legacy and not used for core features. `postRouter` is `publicProcedure` and can write posts without auth — smell for a demo route that still ships. Full tRPC deletion spans S10 (`src/trpc/**`, post component). Within S9, the minimal clarity win is either (1) delete post router + empty the app router if no product use, or (2) add a file-level “LEGACY SCAFFOLDING — do not extend” banner and leave wiring to a later cross-slice cleanup.
- Why not full delete now without S10: breaks `api.post.*` UI and trpc route until those are removed in the same pack.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | LEGACY banners in S9 files; no behavior change | Safe; honest | Still ships dead path | 3/5 | S |
| B | Cross-slice delete: post router + UI + trpc route (coord S10) | Real brevity | Multi-slice | 5/5 | M |
| C | Add auth to post router | Safer demo | Invests in dead feature | 1/5 | S |

### Minimal implementation sketch
- Prefer **A** in S9-only fix pack; recommend **B** in consolidate phase with S10 ownership.
- Files (A): `db.ts`, `trpc.ts`, `root.ts`, `post.ts` — short headers.
- What NOT to do: Grow Prisma models; add new tRPC routers for app features.

### Skills applied
- Base: YAGNI
- Domain: Karpathy simplicity; Exigo dual-DB convention

### Research notes
- AGENTS.md Dual Database System section explicitly deprecates Prisma growth.

### Residual risks
- Public post create remains if only banners applied — low impact (SQLite demo data).

### Suggested finding severity / title (for L1)
- **P2** — Dead T3 tRPC/Prisma scaffolding still in `src/server`
