# Fix pack P12-B — Tutor memory vector search

**Findings:** F-W7-013  
**Status:** done

## What changed

| File | Change |
|------|--------|
| `convex/courseTutor.searchMemories` | Filter by `userId` on vector index; enforce `spaceId` after fetch |
| `convex/courseTutorSearch.ts` | Public `searchMemoriesForSpace` action (auth + ownership) |
| `assembleTutorContext.ts` | Calls vector action instead of load-50 + JS cosine |

Hot path no longer scores all memories in the Next process.
