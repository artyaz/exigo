# Fix pack P11-A — Teach/clarify on resolveAiProvider

**Findings:** F-W7-002 (partial)  
**Status:** done

## What changed

| Route | Change |
|-------|--------|
| `api/learn/teach` | `resolveAiProvider(convex).stream({ prompt })` |
| `api/learn/clarify` | same |

Observability uses `provider.config.label` + model.

## Residual

- Tutor still uses env Gemini for tools/embeddings (function calling not on AiProvider yet)
- tests/generate + validate + knowledge/title still direct Gemini if not already migrated
