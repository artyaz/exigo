# Fix pack P12-A — Remaining product Next routes → resolveAiProvider

**Findings:** F-W7-002 residual  
**Status:** done

## Migrated

| Route | Change |
|-------|--------|
| `api/tests/generate` | `resolveAiProvider` + stream retry on 429 |
| `api/tests/validate` | `provider.generate` (primary + fallback model) |
| `api/tests/feels-hard` | `provider.generate` |
| `api/knowledge/title` | `provider.generate` with model candidates; tests updated |

## Residual

- Tutor tool-calling / embeddings still env Gemini (`getEnvGeminiClient`) — function calling not on `AiProvider` yet.
