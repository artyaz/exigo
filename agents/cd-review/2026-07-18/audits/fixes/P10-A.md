# Fix pack P10-A — Quarantine `default_user` tenancy

**Findings:** F-W7-004  
**Status:** done

## What changed

| File | Change |
|------|--------|
| `convex/spaceAccess.ts` | **New** `DEFAULT_SPACE_OWNER`, `canReadSpace`, `canWriteSpace` |
| `questions`, `knowledgeNodes`, `knowledgePieces`, `deepDives`, `testMessages` | Use shared helpers |
| `tests.getOwnedSpaceIds` | Quota counts **only** the authed user’s spaces (not default_user demos) |

## Product decision

- Keep shared-demo **read** access via `default_user`.
- Writes stay strict owner-only.
- Plan limits never inflate from demo spaces.
