## Decision Package — S7-B003

### Recommendation
- Approach name: **B — Delete dead test-gen residue first; then extract Knowledge tab sections**
- One-paragraph rationale: `spaces/[spaceId]/page.tsx` (~635 lines) mixes shell tabs, knowledge add/bulk import, knowledge-node modal, and a large dead carcass of inlined test generation (state, helpers, icons, `createTestServerAction` import) after `TestGenerateButton`/`TestGrid` extraction. North-star win is mostly *brevity*: delete unused symbols immediately, then split knowledge UI into presentational sections so the page is a thin tab shell like the learn course shell target in S7-B001.
- Why not the alternatives: A (delete-only) is incomplete if we stop mid-god-page. C (rewrite space page as multi-route) is product scope.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Delete dead imports/state only | Fast, safe | Knowledge+modal still heavy in one file | 3/5 | S |
| B | Delete dead + extract KnowledgePanel / NodeModal | Readable shell; matches TestGrid extraction | Slight file churn | 5/5 | M |
| C | Split tabs into route segments | URL-addressable tabs | Product/routing change | 3/5 | L |

### Minimal implementation sketch
- Files to touch:
  - `src/app/spaces/[spaceId]/page.tsx`
  - New: `src/app/_components/spaces/KnowledgeTab.tsx` (or under `spaces/[spaceId]/_components/`)
  - New: `KnowledgeNodeModal.tsx`
  - Optional: `useSpaceData` already local — keep or move with knowledge
- Dead delete list (verified unreferenced beyond declarations):
  - Import `createTestServerAction`
  - `hashCode`, `getUserFacingErrorMessage`
  - State: `testType`, `isGenerating`, `showTypeDropdown`, `showTopicPicker`, `selectedTopicId`, `testGenerateError`, `dropdownRef`, `hoveredTestId`, `testMousePos`
  - `selectType`, `selectedTopic`, `TypeIcon`
  - Icons only used by dead paths: `Clock`, `ChevronDown`, `ListChecks`, `PenLine`, `Shuffle`, `ArrowDown`, `ArrowUp`
- Also fix: Esc kbd affordance without `keydown` handler → wire Escape to close modal or remove the hint.
- What NOT to do:
  - Do not reimplement test generation UI.
  - Do not change knowledge import CSV contracts.

### Skills applied
- Base: brainstorming + YAGNI (delete first)
- Domain: React/UI structure; Karpathy simplicity

### Research notes
- Sibling pattern: tests already extracted to `TestGenerateButton` + `TestGrid`; knowledge was left behind.
- Key takeaway: dead residue is the smoking gun of incomplete extraction.

### Residual risks
- `isAdding` shared between add + bulk — keep single flag or rename when extracting.
- Title auto-gen fire-and-forget fetch stays as-is unless separate finding.

### Suggested finding severity / title (for L1)
- **P1** — Space detail page is a partial-extraction god file with large dead test-gen residue
