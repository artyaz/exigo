# Decision Package — S4-B006

## Issue Packet (input)
- **BRAIN_ID:** S4-B006
- **TRIGGER:** structure / clarity
- **NORTH_STAR_HURT:** readability, clarity, brevity
- **LOCATION:** `runtime/helpers.ts` (294 lines) — scalar math, strings, arrays, obs queries, `sortStep`, grids, Conway `stepLife` in one bag
- **SYMPTOM:** Closed helper surface is good architecture; the *file* mixes timeless DSL primitives with niche simulation helpers. Harder to audit the public API of the language.
- **QUESTION:** Split helpers by domain vs leave one table with section comments?

---

## Decision Package — S4-B006

### Recommendation
- **Approach name:** B — Split into domain modules, re-export single `HELPERS` / `HELPER_NAMES`
- **One-paragraph rationale:** Consumers only need `HELPERS` and `HELPER_NAMES` (expr + validate). Splitting into `helpers/core.ts` (math/string/array/record), `helpers/obs.ts`, `helpers/grid.ts` (grids + life + sortStep) with `helpers.ts` as barrel preserves the closed-set contract while making “what is the language?” answerable in one short file. Section comments alone (C) already exist and still leave a long scroll. Deleting niche helpers (A) is product change without evidence they’re unused.
- **Why not the alternatives:** Delete niche helpers (A) needs usage audit outside S4 ownership risk. Comments only (C) weak win.

### Approaches considered
| ID | Name | Pros | Cons | North-star score | Effort |
|----|------|------|------|------------------|--------|
| A | Delete sortStep/stepLife if unused | Shorter | Product risk, need full repo grep | 3/5 | M |
| B | Domain files + barrel HELPERS | Readable API inventory | More files | **4/5** | M |
| C | Keep one file | No churn | Scroll tax remains | 2/5 | — |

### Minimal implementation sketch
- Files under `runtime/helpers/` or sibling `helpersCore.ts` etc.; keep import path `./helpers` stable for expr/validate
- Steps: move functions, reassemble `HELPERS` object once, keep `CAPS` with core
- What NOT to do: change helper names/signatures; add new helpers

### Skills applied
- architecture: clear units; single public surface
- YAGNI: don’t invent helper registry framework

### Residual risks
- Vitest path imports if any deep-import helpers internals (grep first).

### Suggested finding severity / title (for L1)
- **P2** — Closed helpers are one kitchen-sink module
